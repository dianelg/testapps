_schema-version: "3.2"
ID: nsproddtls
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nsproddtls-destination-content
  type: com.sap.application.content
  requires:
  - name: nsproddtls-destination-service
    parameters:
      content-target: true
  - name: nsproddtls-repo-host
    parameters:
      service-key:
        name: nsproddtls-repo-host-key
  - name: nsproddtls-uaa
    parameters:
      service-key:
        name: nsproddtls-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nsproddtls_html_repo_host
          ServiceInstanceName: nsproddtls-html5-srv
          ServiceKeyName: nsproddtls-repo-host-key
          sap.cloud.service: nsproddtls
        - Authentication: OAuth2UserTokenExchange
          Name: nsproddtls_uaa
          ServiceInstanceName: nsproddtls-xsuaa-srv
          ServiceKeyName: nsproddtls-uaa-key
          sap.cloud.service: nsproddtls
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: nsproddtls-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nsproddtls-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsproddtls.zip
      name: nsproddtls
      target-path: resources/
- name: nsproddtls
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nsproddtls-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: BasicAuthentication
            Name: FIORIAPPS
            ProxyType: Internet
            Type: HTTP
            URL: https://api.cf.us10-001.hana.ondemand.com/
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nsproddtls-destination-service
    service-plan: lite
- name: nsproddtls-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nsproddtls-xsuaa-srv
    service-plan: application
- name: nsproddtls-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nsproddtls-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
